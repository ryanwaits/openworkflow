{
  "name": "@openworkflow/cli",
  "version": "0.1.0",
  "description": "OpenWorkflow CLI - Extensible workflow automation",
  "bin": {
    "openworkflow": "./bin/openworkflow.ts",
    "ow": "./bin/openworkflow.ts"
  },
  "scripts": {
    "build": "bun build src/index.ts --target bun --outdir dist",
    "dev": "bun run src/index.ts",
    "link": "bun link"
  },
  "dependencies": {
    "@openworkflow/gh": "workspace:*",
    "commander": "^12.0.0",
    "chalk": "^5.3.0"
  }
}